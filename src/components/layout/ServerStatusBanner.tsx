import React from 'react';

// Simple component that doesn't render anything
// We're keeping this file in case it's referenced elsewhere
const ServerStatusBanner = () => {
  return null;
};

export default ServerStatusBanner;
